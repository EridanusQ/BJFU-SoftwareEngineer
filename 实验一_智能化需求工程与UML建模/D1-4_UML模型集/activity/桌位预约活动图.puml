@startuml
start
:顾客选择城市、日期和人数;
:系统展示门店、桌位和猫咪状态;
if (存在可预约桌位?) then (是)
  :顾客填写特殊需求;
  :系统执行偏好与容量校验;
  :生成预约单;
  :发送确认通知;
else (否)
  :系统推荐相邻时段或候补;
  if (顾客接受候补?) then (是)
    :登记候补并发送提醒;
  else (否)
    :结束本次预约;
  endif
endif
stop
@enduml
