@startuml
skinparam backgroundColor #F6FBF8
skinparam shadowing false
skinparam defaultFontName Microsoft YaHei
|顾客|
start
:选择门店、日期、人数、桌型;
:查看猫咪出勤与互动限制;
|预约服务|
:查询可预约库存;
if (存在可用桌位?) then (是)
  :校验会员与特殊需求;
  :生成预约草稿;
  :锁定桌位 15 分钟;
  |支付/通知|
  :创建订金支付单;
  if (支付成功?) then (是)
    |预约服务|
    :确认预约并写入 Trace ID;
    |支付/通知|
    :发送预约成功与到店提醒;
    if (通知失败?) then (是)
      :进入通知补偿队列;
    endif
    |店员|
    :到店核销/迟到处理;
    |预约服务|
    :更新预约状态与审计日志;
    stop
  else (否或超时)
    |预约服务|
    :释放桌位锁;
    |顾客|
    :重新选择或登记候补;
    stop
  endif
else (否)
  |顾客|
  :查看候补或更换时段;
  stop
endif
@enduml
